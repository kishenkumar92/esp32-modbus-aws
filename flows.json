[
    {
        "id": "gen_tab",
        "type": "tab",
        "label": "Generator Control",
        "disabled": false,
        "info": "SOC check every 15min from 3pm. 7pm peak load capture (15 readings). Two-period kWh target to 5am."
    },
    {
        "id": "tick_f1",
        "type": "inject",
        "z": "gen_tab",
        "name": "every 10s",
        "props": [{"p": "payload"}],
        "repeat": "10",
        "crontab": "",
        "once": true,
        "onceDelay": "3",
        "topic": "",
        "payload": "",
        "payloadType": "date",
        "x": 120,
        "y": 100,
        "wires": [["read_f1"]]
    },
    {
        "id": "read_f1",
        "type": "edge get nodes",
        "z": "gen_tab",
        "name": "read SOCs + kWh + load",
        "datatype": "STRING",
        "nodeids": [
            {"nodeid": "inv1_soc1"},
            {"nodeid": "inv1_soc2"},
            {"nodeid": "inv2_soc"},
            {"nodeid": "inv3_soc"},
            {"nodeid": "m12_kwh_total"},
            {"nodeid": "m13_ptot"}
        ],
        "x": 320,
        "y": 100,
        "wires": [["logic_f1", "dbg_raw_f1"]]
    },
    {
        "id": "dbg_raw_f1",
        "type": "debug",
        "z": "gen_tab",
        "name": "F1 raw read",
        "active": true,
        "tosidebar": true,
        "console": false,
        "tostatus": false,
        "complete": "payload",
        "targetType": "msg",
        "x": 540,
        "y": 40,
        "wires": []
    },
    {
        "id": "logic_f1",
        "type": "function",
        "z": "gen_tab",
        "name": "F1: SOC protect + evening recovery",
        "func": "var START_SOC   = 20;\nvar STOP_SOC    = 22;\nvar CAPACITY    = 568;\nvar RESERVE     = 20;\nvar LOAD_LATE   = 40;\nvar LOAD_EARLY  = 80;\nvar CUTOVER_HR  = 21;\nvar MORNING_HR  = 7;\nvar BUFFER_SIZE = 6;\n\nvar raw = msg.payload;\nvar socs = [];\nvar kwhNow = null;\nvar loadW = null;\n\nif (typeof raw === 'string') {\n    var parts = raw.split(',').map(function(v) { return parseFloat(v.trim()); });\n    if (parts.length >= 6) { socs = parts.slice(0,4); kwhNow = parts[4]; loadW = parts[5]; }\n    else if (parts.length >= 5) { socs = parts.slice(0,4); kwhNow = parts[4]; }\n    else { socs = parts; }\n} else if (Array.isArray(raw)) {\n    for (var i = 0; i < raw.length; i++) {\n        var id = raw[i].nodeid || '';\n        var val = parseFloat(raw[i].value !== undefined ? raw[i].value : raw[i].payload);\n        if (id === 'm12_kwh_total') kwhNow = val;\n        else if (id === 'm13_ptot') loadW = val;\n        else socs.push(val);\n    }\n}\n\nsocs = socs.filter(function(n) { return !isNaN(n) && n > 0 && n <= 100; });\n\n// Mock data fallback for bench testing (sensors not connected)\nif (socs.length === 0) {\n    var ms = flow.get('mock_soc');\n    if (ms !== null && ms !== undefined) { var msv = parseFloat(ms); if (!isNaN(msv)) socs = [msv,msv,msv,msv]; }\n}\nif (kwhNow === null || isNaN(kwhNow)) {\n    var mk = flow.get('mock_kwh');\n    if (mk !== null && mk !== undefined) kwhNow = parseFloat(mk);\n}\nif (loadW === null || isNaN(loadW)) {\n    var ml = flow.get('mock_load');\n    if (ml !== null && ml !== undefined) loadW = parseFloat(ml);\n}\n\nif (socs.length === 0) { node.status({fill:'grey', text:'no SOC'}); return [null, null]; }\n\nvar avg = socs.reduce(function(a,b){return a+b;},0) / socs.length;\nflow.set('last_avg_soc', avg);\nif (kwhNow !== null && !isNaN(kwhNow)) flow.set('last_kwh', kwhNow);\n\nif (loadW !== null && !isNaN(loadW) && loadW > 0) {\n    var buf = flow.get('load_buffer') || [];\n    buf.push(loadW);\n    if (buf.length > BUFFER_SIZE * 6) buf = buf.slice(-BUFFER_SIZE * 6);\n    flow.set('load_buffer', buf);\n}\n\nvar gen         = flow.get('gen_state')        || 0;\nvar eveningMode = flow.get('evening_mode')     || false;\nvar eveningDone = flow.get('evening_complete') || false;\n\nif (avg <= START_SOC) {\n    flow.set('gen_state', 1);\n    node.status({fill:'red', text:'LOW SOC ' + avg.toFixed(1) + '% - gen ON'});\n    return [{payload:1, topic:'DO01'}, null];\n}\n\nif (avg >= STOP_SOC && gen === 1 && !eveningMode) {\n    flow.set('gen_state', 0);\n    node.status({fill:'green', text:'SOC OK ' + avg.toFixed(1) + '% - gen OFF'});\n    return [{payload:0, topic:'DO01'}, null];\n}\n\nvar now = new Date();\nvar h = now.getHours();\nvar m = now.getMinutes();\nvar inWindow = (h >= 18 || h < MORNING_HR);\n\nif (inWindow && !eveningMode && !eveningDone && gen === 0) {\n    var avail = ((avg - RESERVE) / 100) * CAPACITY;\n    if (avail < 0) avail = 0;\n\n    var buf2 = flow.get('load_buffer') || [];\n    var lkw;\n    if (buf2.length >= BUFFER_SIZE) {\n        var rec = buf2.slice(-BUFFER_SIZE);\n        lkw = (rec.reduce(function(a,b){return a+b;},0) / rec.length) / 1000;\n    } else {\n        lkw = (h >= CUTOVER_HR || h < MORNING_HR) ? LOAD_LATE : LOAD_EARLY;\n    }\n\n    var hrs;\n    if (h >= CUTOVER_HR) { hrs = (24 - h - (m/60)) + MORNING_HR; }\n    else if (h < MORNING_HR) { hrs = MORNING_HR - h - (m/60); }\n    else { hrs = 0; }\n\n    var need = hrs * lkw;\n\n    if (avail < need && kwhNow !== null && !isNaN(kwhNow)) {\n        var deficit = need - avail;\n        var target = kwhNow + deficit;\n        flow.set('kwh_snapshot', kwhNow);\n        flow.set('kwh_target', target);\n        flow.set('evening_mode', true);\n        flow.set('evening_fixed_stop', false);\n        flow.set('gen_state', 1);\n        node.status({fill:'orange', text:'RECOVERY avg=' + avg.toFixed(1) + '% def=' + deficit.toFixed(0) + 'kWh tgt=' + target.toFixed(0)});\n        return [{payload:1, topic:'DO01'}, {payload:'start_poll'}];\n    }\n}\n\nnode.status({\n    fill: gen ? 'red' : 'green',\n    text: 'avg=' + avg.toFixed(1) + '% gen=' + (gen?'ON':'OFF') + (eveningMode?' [evening]':'') + (eveningDone?' [done]':'')\n});\nreturn [null, null];",
        "outputs": 2,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 580,
        "y": 100,
        "wires": [
            ["write_do1_f1", "dbg_out_f1"],
            ["tick_poll"]
        ]
    },
    {
        "id": "write_do1_f1",
        "type": "edge set node",
        "z": "gen_tab",
        "name": "set DO01",
        "nodeid": "DO01",
        "x": 860,
        "y": 60,
        "wires": [[]]
    },
    {
        "id": "dbg_out_f1",
        "type": "debug",
        "z": "gen_tab",
        "name": "F1 decision",
        "active": true,
        "tosidebar": true,
        "console": false,
        "tostatus": false,
        "complete": "payload",
        "targetType": "msg",
        "x": 860,
        "y": 120,
        "wires": []
    },
    {
        "id": "tick_soc_check",
        "type": "inject",
        "z": "gen_tab",
        "name": "SOC check every 15min (2pm-midnight)",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "*/15 14-23 * * *",
        "once": false,
        "topic": "",
        "payload": "",
        "payloadType": "date",
        "x": 180,
        "y": 280,
        "wires": [["logic_soc_check"]]
    },
    {
        "id": "logic_soc_check",
        "type": "function",
        "z": "gen_tab",
        "name": "SOC check: start gen if needed",
        "func": "// Daily reset on first tick at/after 3pm\nvar today = new Date().toDateString();\nif (flow.get('last_reset_date') !== today) {\n    flow.set('last_reset_date', today);\n    flow.set('evening_complete', false);\n    flow.set('evening_fixed_stop', false);\n    flow.set('peak_captured', false);\n    flow.set('peak_load_kw', null);\n    flow.set('peak_buffer', []);\n    node.warn('Daily state reset');\n}\n\n// Guards\nif (flow.get('gen_state') === 1) {\n    node.status({fill:'grey', text:'gen already on'});\n    return null;\n}\n\nvar avg = flow.get('last_avg_soc');\nif (avg === null || avg === undefined) {\n    node.status({fill:'grey', text:'no SOC data'});\n    return null;\n}\n\nvar h = new Date().getHours();\nvar mockH = flow.get('mock_hour');\nif (mockH !== null && mockH !== undefined) h = parseInt(mockH);\n\n// Simple threshold: 2pm-3pm = 50%, 3pm-4pm = 60%, 4pm onwards = 90%\nvar threshold = (h < 15) ? 50 : (h < 16) ? 60 : 90;\nif (avg >= threshold) {\n    node.status({fill:'green', text:'SOC ' + avg.toFixed(1) + '% >= ' + threshold + '% - OK'});\n    return null;\n}\n\n// SOC is below threshold - start generator\nflow.set('gen_state', 1);\nflow.set('evening_mode', true);\nflow.set('evening_fixed_stop', false);\nflow.set('evening_complete', false);\nflow.set('start_soc', avg);\nvar kwhNow = flow.get('last_kwh') || 0;\nflow.set('kwh_snapshot', kwhNow);\n\nnode.status({fill:'orange', text:'SOC ' + avg.toFixed(1) + '% < ' + threshold + '% - gen ON'});\nreturn {payload: 1, topic: 'DO01'};",
        "outputs": 1,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 460,
        "y": 280,
        "wires": [["write_do1_soc", "dbg_soc_check", "tick_poll"]]
    },
    {
        "id": "write_do1_soc",
        "type": "edge set node",
        "z": "gen_tab",
        "name": "set DO01",
        "nodeid": "DO01",
        "x": 760,
        "y": 260,
        "wires": [[]]
    },
    {
        "id": "dbg_soc_check",
        "type": "debug",
        "z": "gen_tab",
        "name": "SOC check decision",
        "active": true,
        "tosidebar": true,
        "console": false,
        "tostatus": false,
        "complete": "payload",
        "targetType": "msg",
        "x": 780,
        "y": 300,
        "wires": []
    },
    {
        "id": "tick_peak_capture",
        "type": "inject",
        "z": "gen_tab",
        "name": "7pm peak capture (every 1min)",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "*/1 19 * * *",
        "once": false,
        "topic": "",
        "payload": "",
        "payloadType": "date",
        "x": 170,
        "y": 420,
        "wires": [["read_peak"]]
    },
    {
        "id": "read_peak",
        "type": "edge get nodes",
        "z": "gen_tab",
        "name": "read site load",
        "datatype": "STRING",
        "nodeids": [
            {"nodeid": "m13_ptot"}
        ],
        "x": 390,
        "y": 420,
        "wires": [["logic_peak_capture"]]
    },
    {
        "id": "logic_peak_capture",
        "type": "function",
        "z": "gen_tab",
        "name": "7pm peak load capture",
        "func": "if (flow.get('peak_captured')) {\n    node.status({fill:'green', text:'peak already captured'});\n    return null;\n}\n\nvar raw = msg.payload;\nvar loadW = null;\n\nif (typeof raw === 'string') {\n    loadW = parseFloat(raw.trim());\n} else if (Array.isArray(raw)) {\n    for (var i = 0; i < raw.length; i++) {\n        var id = raw[i].nodeid || '';\n        if (id === 'm13_ptot') {\n            loadW = parseFloat(raw[i].value !== undefined ? raw[i].value : raw[i].payload);\n        }\n    }\n} else if (raw && typeof raw === 'object') {\n    loadW = parseFloat(raw['m13_ptot']);\n}\n\nif (loadW === null || isNaN(loadW) || loadW <= 0) {\n    node.status({fill:'yellow', text:'bad read - skipping'});\n    return null;\n}\n\nvar buf = flow.get('peak_buffer') || [];\nbuf.push(loadW);\nflow.set('peak_buffer', buf);\n\nif (buf.length >= 15) {\n    var avg = buf.reduce(function(a,b){return a+b;},0) / buf.length;\n    var peakKw = avg / 1000;\n    flow.set('peak_load_kw', peakKw);\n    flow.set('peak_captured', true);\n    flow.set('peak_buffer', []);\n    node.status({fill:'blue', text:'peak captured ' + peakKw.toFixed(1) + 'kW (15 readings)'});\n} else {\n    node.status({fill:'yellow', text:'collecting ' + buf.length + '/15'});\n}\nreturn null;",
        "outputs": 1,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 600,
        "y": 420,
        "wires": [["dbg_peak"]]
    },
    {
        "id": "dbg_peak",
        "type": "debug",
        "z": "gen_tab",
        "name": "peak capture",
        "active": true,
        "tosidebar": true,
        "console": false,
        "tostatus": false,
        "complete": "payload",
        "targetType": "msg",
        "x": 790,
        "y": 420,
        "wires": []
    },
    {
        "id": "tick_poll",
        "type": "trigger",
        "z": "gen_tab",
        "name": "60s poll trigger",
        "op1": "",
        "op2": "1",
        "op1type": "nul",
        "op2type": "str",
        "duration": "60",
        "extend": false,
        "overrideDelay": false,
        "units": "s",
        "reset": "",
        "bytopic": "all",
        "x": 160,
        "y": 560,
        "wires": [["read_poll"]]
    },
    {
        "id": "read_poll",
        "type": "edge get nodes",
        "z": "gen_tab",
        "name": "read gen kWh + site load",
        "datatype": "STRING",
        "nodeids": [
            {"nodeid": "m12_kwh_total"},
            {"nodeid": "m13_ptot"}
        ],
        "x": 360,
        "y": 560,
        "wires": [["logic_poll"]]
    },
    {
        "id": "logic_poll",
        "type": "function",
        "z": "gen_tab",
        "name": "kWh target stop (two-period model)",
        "func": "var MAX_FAIL    = 3;\nvar BUFFER_SIZE = 6;\nvar LOAD_LATE   = 40;\nvar LOAD_EARLY  = 80;\nvar CUTOVER_HR  = 21;\nvar MORNING_HR  = 5;\n\nvar eveningMode = flow.get('evening_mode') || false;\nif (!eveningMode) { node.status({fill:'grey', text:'idle'}); return null; }\n\nvar now = new Date();\nvar h = now.getHours();\nvar m = now.getMinutes();\n\nvar raw = msg.payload;\nvar kwhNow = null;\nvar loadW = null;\n\nif (typeof raw === 'string') {\n    var parts = raw.split(',').map(function(v){return parseFloat(v.trim());});\n    kwhNow = parts[0]; loadW = parts[1];\n} else if (Array.isArray(raw)) {\n    for (var i = 0; i < raw.length; i++) {\n        var id = raw[i].nodeid || '';\n        var val = parseFloat(raw[i].value !== undefined ? raw[i].value : raw[i].payload);\n        if (id === 'm12_kwh_total') kwhNow = val;\n        else if (id === 'm13_ptot') loadW = val;\n    }\n} else if (raw && typeof raw === 'object') {\n    kwhNow = parseFloat(raw['m12_kwh_total']);\n    loadW  = parseFloat(raw['m13_ptot']);\n}\n\n// Mock data fallback for bench testing (sensors not connected)\nif (kwhNow === null || isNaN(kwhNow)) {\n    var mk = flow.get('mock_kwh');\n    if (mk !== null && mk !== undefined) kwhNow = parseFloat(mk);\n}\nif (loadW === null || isNaN(loadW)) {\n    var ml = flow.get('mock_load');\n    if (ml !== null && ml !== undefined) loadW = parseFloat(ml);\n}\n\nvar failCount = context.get('fail_count') || 0;\nif (kwhNow === null || isNaN(kwhNow)) {\n    failCount++;\n    context.set('fail_count', failCount);\n    node.status({fill:'yellow', text:'bad read ' + failCount + '/' + MAX_FAIL});\n    if (failCount < MAX_FAIL) return [null, {payload:'retry'}];\n    return null;\n}\ncontext.set('fail_count', 0);\n\n// Update rolling load buffer from m13\nif (loadW !== null && !isNaN(loadW) && loadW > 0) {\n    var buf = flow.get('load_buffer') || [];\n    buf.push(loadW);\n    if (buf.length > BUFFER_SIZE) buf = buf.slice(-BUFFER_SIZE);\n    flow.set('load_buffer', buf);\n}\n\n// Rolling average load (m13)\nvar buf2 = flow.get('load_buffer') || [];\nvar lkw;\nif (buf2.length >= BUFFER_SIZE) {\n    lkw = (buf2.reduce(function(a,b){return a+b;},0) / buf2.length) / 1000;\n} else {\n    lkw = (h >= CUTOVER_HR || h < MORNING_HR) ? LOAD_LATE : LOAD_EARLY;\n}\n\n// Two-period need calculation\n// After 7pm peak is captured: peak_lkw until 9pm, 2/3 of that from 9pm to 5am\n// Before 7pm: flat rolling average to 5am\nvar peakCaptured = flow.get('peak_captured') || false;\nvar peakLkw      = flow.get('peak_load_kw')  || null;\nvar need;\n\nif (peakCaptured && peakLkw) {\n    var offpeakLkw = peakLkw * (2/3);\n    var peakHrs, offpeakHrs;\n    if (h < CUTOVER_HR) {\n        peakHrs    = CUTOVER_HR - h - (m/60);\n        offpeakHrs = 8;\n    } else {\n        peakHrs    = 0;\n        offpeakHrs = (h >= CUTOVER_HR) ? (24 - h - (m/60) + MORNING_HR) : (MORNING_HR - h - (m/60));\n    }\n    need = (peakHrs * peakLkw) + (offpeakHrs * offpeakLkw);\n} else {\n    var hrs;\n    if (h < MORNING_HR) { hrs = MORNING_HR - h - (m/60); }\n    else { hrs = (24 - h - (m/60)) + MORNING_HR; }\n    need = hrs * lkw;\n}\n\nvar avg = flow.get('last_avg_soc') || 0;\nvar avail = ((avg - 20) / 100) * 568;\nif (avail < 0) avail = 0;\n\n// Stop early if battery has recovered +10% from when generator started\n// (weather improved - solar doing the work, no need to keep running)\nvar startSoc = flow.get('start_soc') || 0;\nif (startSoc > 0 && avg >= startSoc + 10) {\n    flow.set('evening_mode', false);\n    flow.set('evening_complete', false);\n    flow.set('gen_state', 0);\n    node.status({fill:'green', text:'SOC +10% recovery (' + startSoc.toFixed(0) + '%->' + avg.toFixed(0) + '%) - gen OFF'});\n    return [{payload:0, topic:'DO01'}, null];\n}\n\nvar deficit = need - avail;\nif (deficit < 0) deficit = 0;\nvar snapshot = flow.get('kwh_snapshot') || kwhNow;\nvar target   = snapshot + deficit;\nflow.set('kwh_target', target);\n\nvar progress   = kwhNow - snapshot;\nvar modelLabel = peakCaptured ? '2p' : 'flat';\nnode.status({fill:'blue', text:'[' + modelLabel + '] kW=' + lkw.toFixed(1) + ' need=' + need.toFixed(0) + ' avail=' + avail.toFixed(0) + ' prog=' + progress.toFixed(0) + '/' + deficit.toFixed(0) + 'kWh'});\n\nif (kwhNow >= target) {\n    flow.set('evening_mode', false);\n    flow.set('evening_complete', true);\n    flow.set('gen_state', 0);\n    node.status({fill:'green', text:'target reached ' + kwhNow.toFixed(0) + '/' + target.toFixed(0) + ' - gen OFF'});\n    return [{payload:0, topic:'DO01'}, null];\n}\nreturn [null, {payload:'continue'}];",
        "outputs": 2,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 580,
        "y": 560,
        "wires": [
            ["write_do1_poll", "dbg_poll"],
            ["tick_poll"]
        ]
    },
    {
        "id": "write_do1_poll",
        "type": "edge set node",
        "z": "gen_tab",
        "name": "set DO01",
        "nodeid": "DO01",
        "x": 840,
        "y": 540,
        "wires": [[]]
    },
    {
        "id": "dbg_poll",
        "type": "debug",
        "z": "gen_tab",
        "name": "poll decision",
        "active": true,
        "tosidebar": true,
        "console": false,
        "tostatus": false,
        "complete": "payload",
        "targetType": "msg",
        "x": 840,
        "y": 580,
        "wires": []
    },
    {
        "id": "test_panel",
        "type": "inject",
        "z": "gen_tab",
        "name": "TEST PANEL - click to cycle",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "",
        "once": false,
        "topic": "",
        "payload": "",
        "payloadType": "date",
        "x": 180,
        "y": 700,
        "wires": [["test_logic"]]
    },
    {
        "id": "test_logic",
        "type": "function",
        "z": "gen_tab",
        "name": "test scenarios",
        "func": "var scenario = context.get('scenario') || 0;\nscenario++;\nif (scenario > 6) scenario = 1;\ncontext.set('scenario', scenario);\n\nif (scenario === 1) {\n    // F1 low-SOC: mock SOC=18% -> expect DO01=1 within 10s\n    flow.set('mock_hour', null);\n    flow.set('mock_soc', 18);\n    flow.set('mock_kwh', 27000);\n    flow.set('mock_load', 40000);\n    flow.set('evening_mode', false);\n    flow.set('evening_complete', false);\n    flow.set('gen_state', 0);\n    flow.set('last_avg_soc', 18);\n    node.status({fill:'red', text:'S1: SOC=18% - expect DO01=1 within 10s (F1)'});\n\n} else if (scenario === 2) {\n    // F1 stop: mock SOC=23%, gen ON -> expect DO01=0 within 10s\n    flow.set('mock_hour', null);\n    flow.set('mock_soc', 23);\n    flow.set('mock_kwh', 27000);\n    flow.set('mock_load', 40000);\n    flow.set('evening_mode', false);\n    flow.set('evening_complete', false);\n    flow.set('gen_state', 1);\n    flow.set('last_avg_soc', 23);\n    node.status({fill:'green', text:'S2: SOC=23% gen ON - expect DO01=0 within 10s (F1)'});\n\n} else if (scenario === 3) {\n    // SOC check start: SOC=85%, gen off. Use time simulation buttons to set the hour, then trigger tick_soc_check.\n    // 4pm+ sim -> DO01=1 (85% < 90%). 2pm or 3pm sim -> no DO01 (85% >= 50/60%).\n    flow.set('mock_soc', null);\n    flow.set('mock_hour', null);\n    flow.set('mock_kwh', 27000);\n    flow.set('mock_load', 60000);\n    flow.set('evening_mode', false);\n    flow.set('evening_complete', false);\n    flow.set('gen_state', 0);\n    flow.set('last_avg_soc', 85);\n    flow.set('last_kwh', 27000);\n    flow.set('load_buffer', [60000,60000,60000,60000,60000,60000]);\n    flow.set('last_reset_date', new Date().toDateString());\n    node.status({fill:'orange', text:'S3 ready: click a TIME button, then trigger tick_soc_check'});\n\n} else if (scenario === 4) {\n    // kWh stop: mock_kwh=9999 >> any calculated target -> expect DO01=0 within 60s\n    flow.set('mock_kwh', 9999);\n    flow.set('mock_load', 40000);\n    flow.set('evening_mode', true);\n    flow.set('evening_fixed_stop', false);\n    flow.set('evening_complete', false);\n    flow.set('gen_state', 1);\n    flow.set('kwh_snapshot', 1000);\n    flow.set('last_avg_soc', 50);\n    flow.set('peak_captured', true);\n    flow.set('peak_load_kw', 50);\n    node.status({fill:'blue', text:'S4: mock kWh=9999 >> target - expect DO01=0 within 60s (poll)'});\n    return {payload: 'start_poll'};\n\n} else if (scenario === 5) {\n    // +10% SOC recovery stop: SOC has improved 13% since gen started -> poll should stop gen\n    flow.set('mock_kwh', 27000);\n    flow.set('mock_load', 40000);\n    flow.set('mock_soc', null);\n    flow.set('last_avg_soc', 61);\n    flow.set('start_soc', 48);\n    flow.set('evening_mode', true);\n    flow.set('evening_complete', false);\n    flow.set('gen_state', 1);\n    flow.set('kwh_snapshot', 26900);\n    flow.set('peak_captured', false);\n    node.status({fill:'green', text:'S5: SOC 48->61% (+13%) - expect DO01=0 within 60s (poll +10% stop)'});\n    return {payload: 'start_poll'};\n\n} else if (scenario === 6) {\n    // F1 should NOT stop gen when evening session is active\n    // SOC recovers to 25% but evening_mode=true means F1 must not interfere\n    flow.set('mock_soc', 25);\n    flow.set('mock_kwh', 27000);\n    flow.set('mock_load', 40000);\n    flow.set('gen_state', 1);\n    flow.set('evening_mode', true);\n    flow.set('evening_complete', false);\n    flow.set('last_avg_soc', 25);\n    node.status({fill:'purple', text:'S6: SOC=25% gen ON evening mode - wait 10s - F1 should NOT fire DO01=0'});\n}\nreturn null;",
        "outputs": 1,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 420,
        "y": 700,
        "wires": [["tick_poll"]]
    },

    {
        "id": "set_mock_hour",
        "type": "function",
        "z": "gen_tab",
        "name": "set mock hour",
        "func": "flow.set('mock_hour', msg.payload);\nif (msg.payload === null) {\n    node.status({fill:'grey', text:'mock_hour cleared - using real clock'});\n} else {\n    var labels = {14:'2pm (50% threshold)', 15:'3pm (60% threshold)', 16:'4pm+ (90% threshold)'};\n    node.status({fill:'blue', text:'simulating ' + (labels[msg.payload] || msg.payload + ':00')});\n}\nreturn null;",
        "outputs": 1,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 420,
        "y": 820,
        "wires": [[]]
    },

    {
        "id": "sim_2pm",
        "type": "inject",
        "z": "gen_tab",
        "name": "Simulate 2pm (50% threshold)",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "",
        "once": false,
        "topic": "",
        "payload": "14",
        "payloadType": "num",
        "x": 170,
        "y": 780,
        "wires": [["set_mock_hour"]]
    },
    {
        "id": "sim_3pm",
        "type": "inject",
        "z": "gen_tab",
        "name": "Simulate 3pm (60% threshold)",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "",
        "once": false,
        "topic": "",
        "payload": "15",
        "payloadType": "num",
        "x": 170,
        "y": 820,
        "wires": [["set_mock_hour"]]
    },
    {
        "id": "sim_4pm",
        "type": "inject",
        "z": "gen_tab",
        "name": "Simulate 4pm+ (90% threshold)",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "",
        "once": false,
        "topic": "",
        "payload": "16",
        "payloadType": "num",
        "x": 170,
        "y": 860,
        "wires": [["set_mock_hour"]]
    },
    {
        "id": "sim_clear",
        "type": "inject",
        "z": "gen_tab",
        "name": "Clear - use real clock",
        "props": [{"p": "payload"}],
        "repeat": "",
        "crontab": "",
        "once": false,
        "topic": "",
        "payload": "",
        "payloadType": "null",
        "x": 150,
        "y": 900,
        "wires": [["set_mock_hour"]]
    }
]
